// PostCSS configuration for the Nuxt rhythm app.
//
// This file configures PostCSS to use Tailwind CSS and
// Autoprefixer. PostCSS runs as part of the Nuxt build process
// and transforms the Tailwind directives in the CSS file into
// actual CSS rules.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
